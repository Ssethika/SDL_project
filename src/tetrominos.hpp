#include "square.hpp"
// TODO: Class for tetrominos: we will use an abstract factory to make the different tetrominos


enum class Type {
	LSHAPE,
	SQUARESHAPE,
};

class TetrominosBuilder {

};

class Tetrominos {
	Type type;
	SQUARECOLOR color;
};
